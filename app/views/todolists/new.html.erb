<h1>New List</h1>
<%= form_with model: @list, url:'/todolists', local:true do |f| %>
  <h4>Titile</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <h4>Picture</h4>
  <%= f.file_field :image %>
  <%= f.submit 'Submit', class:"btn btn-success" %>
<% end %>